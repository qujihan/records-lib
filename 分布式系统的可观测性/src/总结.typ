#import "../lib.typ":*
= 总结

我的朋友Brian Knox是DigitalOcean作为可观测团队的负责人，它说过：
#ref(
  "",
)[
  可观测性团队的目标并不是收集Logs、Metrics以及Traces。其核心是构建一种基于事实以及反馈的工程文化,并在更广泛的组织内传播这种文化。

  #align(right)[—— Brian Knox]
]

可观测其本身也是如此，它并不是Logs、Metrics以及Traces的集合，而是在调试过程中，由数据驱动，并使用反馈来迭代以及改进产品的过程。